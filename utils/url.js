// third party lib
function parse(url, parseQueryString, slashesDenoteHost) {

    // doing work ...

    return {
        protocol: 'protocol';
        hostname: 'hostname';
        pathname: 'pathname';
    };
}